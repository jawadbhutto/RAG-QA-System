# --- Document loading & chunking ---
pypdf>=5.1.0
langchain-community>=0.3.19
langchain-text-splitters>=0.3.6
langchain-core>=0.3.34

# --- Embeddings & vector store ---
langchain-chroma>=0.2.2
langchain-huggingface>=0.1.2
sentence-transformers>=3.4.1
chromadb>=0.6.3

# --- LLM (answer generation) ---
# `langchain` provides init_chat_model, the provider-agnostic factory
# used in src/llm.py. Install the provider package matching whichever
# LLM_MODEL you configure in .env. Default here is a local Ollama model
# (no API key, runs on your own machine — install Ollama separately
# from https://ollama.com and run `ollama pull qwen2.5:3b`).
langchain>=0.3.19
langchain-groq>=1.1.2
#langchain-ollama>=0.2.3         # for local "ollama:..." models
# langchain-openai>=0.3.7      # for "openai:..." models
# langchain-anthropic>=0.3.7   # for "anthropic:..." models

# --- App / config ---
streamlit>=1.41.1
pydantic>=2.10.6
pydantic-settings>=2.7.1
python-dotenv>=1.0.1

# --- Dev / testing ---
pytest>=8.3.4
